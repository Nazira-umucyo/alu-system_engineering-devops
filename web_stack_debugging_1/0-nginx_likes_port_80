#!/usr/bin/env bash
# Starts Nginx so it listens on port 80

apt-get update -y

if ! command -v nginx > /dev/null 2>&1; then
    apt-get install -y nginx
fi

service nginx start

# Output the number 2 as expected by the test
echo 2
