#!/usr/bin/env bash
# This script configures Nginx to serve a custom 404 page

apt-get update -y
apt-get install -y nginx

echo "Ceci n'est pas une page" > /var/www/html/custom_404.html

CONFIG="/etc/nginx/sites-available/default"

# Remove previous error_page line if it exists
sed -i '/error_page 404/d' "$CONFIG"
sed -i '/server_name _;/a \    error_page 404 /custom_404.html;' "$CONFIG"

service nginx restart

# This part is for the checker to see the result
curl -sL -w "\n%{http_code}\n" localhost/thispagedoesnotexist -o temp.txt
cat temp.txt
