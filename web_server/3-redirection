#!/usr/bin/env bash
# Configure Nginx to redirect /redirect_me permanently to a target URL on a fresh Ubuntu machine

set -e

apt-get update -y
apt-get install -y nginx

service nginx stop || true

cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.bak

# Remove existing location /redirect_me block (from location line to closing brace)
sed -i '/location \/redirect_me {/,/}/d' /etc/nginx/sites-available/default

# Insert the new location block before the last closing brace of the server block
sed -i '/^}/i location /redirect_me {\n\treturn 301 https://www.youtube.com/watch?v=QH2-TGUlwu4;\n}' /etc/nginx/sites-available/default

service nginx start

sleep 2
